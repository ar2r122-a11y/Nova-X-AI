{
  "name": "@nova-x-ai/plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Nova X AI Plugin Engine — sandbox loading, capability security, hook registry, isolated storage",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@nova-x-ai/core": "workspace:*",
    "@nova-x-ai/storage": "workspace:*",
    "@nova-x-ai/security": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
