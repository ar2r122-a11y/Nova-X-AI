{
  "name": "@nova-x-ai/diagnostics",
  "version": "0.1.0",
  "private": true,
  "description": "Nova X AI Diagnostics Engine — health probing, telemetry, metric aggregation, anomaly detection, and profiling",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@nova-x-ai/core": "workspace:*",
    "@nova-x-ai/storage": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
