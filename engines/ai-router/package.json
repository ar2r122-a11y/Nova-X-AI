{
  "name": "@nova-x-ai/ai-router",
  "version": "0.1.0",
  "private": true,
  "description": "AI Router — provider abstraction, routing, and lifecycle for Nova X AI",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
